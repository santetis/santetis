name: Deploy staging admin dashboard

on:
  push:
    paths:
      - frontends/admin_dashboard/**
      - packages/san_router/**
      - common/network_entities/**
      - .github/workflows/deploy_admin_dashboard_staging.yaml
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: "8.x"
      - uses: subosito/flutter-action@v1
        with:
          channel: "beta"
      - name: "Enable Flutter web"
        run: flutter config --enable-web
      - name: "Get dependencies"
        run: flutter packages get
        working-directory: ./frontends/admin_dashboard
      - name: "Flutter build"
        run: flutter build web -t lib/production_main.dart
        working-directory: ./frontends/admin_dashboard
      - name: Deploy to Firebase
        uses: w9jds/firebase-action@master
        with:
          args: deploy --only hosting:staging-admin-dashboard
        env:
          FIREBASE_TOKEN: ${{ secrets.FIREBASE_TOKEN }}
          PROJECT_PATH: ./frontends/admin_dashboard
