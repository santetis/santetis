name: admin_dashboard
description: A new Flutter project.

version: 1.0.0+1

environment:
  sdk: ">=2.2.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^0.1.2
  pedantic: ^1.8.0+1
  provider: ^4.0.2
  shared_preferences: ^0.5.6+1
  mobx: ^0.4.0+4
  flutter_mobx: ^0.3.6+2
  dio: ^3.0.8
  network_entities:
    path: ../../common/network_entities
  san_router:
    path: ../../packages/san_router

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_driver:
    sdk: flutter
  test: ^1.5.0
  mobx_codegen: ^0.4.2
  build_runner: ^1.7.3

flutter:
  uses-material-design: true
